.container-fluid.add-content-group-form
	.row
		.modal-header
			h2 Add Content Group to {{addContentGroupData.website}}
		.modal-body
			form
				.col-xs-12
					.form-group Select Content Group: &nbsp;
						select(ng-model="addContentGroupId")
							option(ng-repeat="group in contentGroups", value="{{group._id}}") {{group.name}}
				.col-xs-12
					button.btn.btn-default.pull-right(ng-click='addContentGroup(addContentGroupData.website, addContentGroupId)') Add Content Group
					button.btn.btn-default.pull-right(ng-click='cancel()') Cancel