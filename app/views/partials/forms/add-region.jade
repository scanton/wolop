.container-fluid.add-content-group-form
	.row
		.modal-header
			h2 Add Region to {{addRegionData.website}}
		.modal-body
			form
				.col-xs-12
					.form-group
						input.form-control(ng-model="addRegionData.name", name='name', type='text', placeholder='Name')
				.col-xs-12
					.form-group
						input.form-control(ng-model="addRegionData.slug", name='slug', type='text', placeholder='slug')
				.col-xs-12
					button.btn.btn-default.pull-right(ng-click='addRegion(addRegionData)') Create Region
					button.btn.btn-default.pull-right(ng-click='cancel()') Cancel