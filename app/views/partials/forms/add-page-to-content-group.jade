.container-fluid.add-page-content-group-form
	.row
		.modal-header
			h2 Add Page to {{groupData.name}}
		.modal-body
			form
				.col-xs-12
					.form-group
						input.form-control(ng-model="addPageData.name", name='name', type='text', placeholder='Name')
				.col-xs-12
					.form-group
						input.form-control(ng-model="addPageData.slug", name='slug', type='text', placeholder='slug')
				.col-xs-12
					button.btn.btn-default.pull-right(ng-click='addPageToGroup(addPageData, groupData._id)') Create Page in {{groupData.name}}
					button.btn.btn-default.pull-right(ng-click='cancel()') Cancel
