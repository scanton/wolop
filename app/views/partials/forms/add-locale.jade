.container-fluid.add-content-group-form
	.row
		.modal-header
			h2 Add Locale to {{addLocaleData.website}}
		.modal-body
			form
				.col-xs-12
					.form-group
						input.form-control(ng-model="addLocaleData.name", name='name', type='text', placeholder='Name')
				.col-xs-12
					.form-group
						input.form-control(ng-model="addLocaleData.slug", name='slug', type='text', placeholder='slug')
				.col-xs-12
					button.btn.btn-default.pull-right(ng-click='addLocale(addLocaleData)') Create Locale
					button.btn.btn-default.pull-right(ng-click='cancel()') Cancel