.container-fluid.add-menu-to-content-group-form
	.row
		.modal-header
			h2 Add Menu to {{groupData.name}}
		.modal-body
			form
				.col-xs-12
					.form-group
						input.form-control(ng-model="addMenuData.name", name='name', type='text', placeholder='Name')
				.col-xs-12
					.form-group
						input.form-control(ng-model="addMenuData.slug", name='slug', type='text', placeholder='slug')
				.col-xs-12
					button.btn.btn-default.pull-right(ng-click='addMenuToGroup(addMenuData, groupData._id)') Create Menu in {{groupData.name}}
					button.btn.btn-default.pull-right(ng-click='cancel()') Cancel
